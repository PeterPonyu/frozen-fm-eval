# Open questions

## Monocells Pages (`prd-monocells-pages`) - 2026-08-13

Planner-locked in `.omx/plans/prd-monocells-pages.md` (do not re-ask unless Critic overrules): blob URLs for LICENSE/PROTOCOL; two-block citation; canonical inner-page hashes; `id` spelling `move-i`; README/PROTOCOL/DESIGN copy only; F3 table skeleton; no JS; font fallbacks first; v1 code-complete without live Pages; no README homepage in v1.

Still open (execution-time or author-owned):

- [ ] Which Zenodo files become `d-vocab` / `d-batch` / `d-spatial` — empty 16:9 frames until a later archive listing; never `paper/` — v1 can ship without images
- [ ] Finding-box titles after PeerJ acceptance vs current `DESIGN.md` paraphrases — v1 frozen to DESIGN cell index; owner: author
- [ ] Venue URL when (if) PeerJ or a preprint exists — do not invent; `mc-status` stays under review
- [ ] Custom domain — none today; no `CNAME` in v1
- [ ] Self-host OFL fonts vs system fallbacks if CSS budget/license is unclear at implementation — plan prefers fallbacks unless OFL files + size gate are met
- [ ] `docs/404.html` in v1 vs later — DESIGN says later
- [ ] Author affiliation on site beyond name + ORCID — DESIGN: name + ORCID only unless citation metadata grows
- [ ] Enabling Pages in Settings (`main` `/docs`) — operator-owned; not a git file; not a v1 merge gate
- [ ] README / `CITATION.cff` / GitHub `homepage` after live 200 — post-go-live, out of v1
- [ ] Analytics / dark mode — v1 none (already decided; listed so they are not “forgotten” into scope)
