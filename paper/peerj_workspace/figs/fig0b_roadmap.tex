% Argument roadmap (hand-authored TikZ), HORIZONTAL layout: the dispute (top) fans into three
% findings side by side --- each fed by its dose-response probe (header) --- converging on the
% deliverable (bottom). Landscape aspect keeps it short at full \textwidth (the vertical 5-box
% stack was too tall once widened). Numbered finding badges drawn in the findings colour.
{\hyphenpenalty=10000\exhyphenpenalty=10000\relax%
\def\fbadge#1{\tikz[baseline=-3pt]{\node[circle,fill=cFind,text=white,inner sep=0pt,minimum size=8.5pt,font=\scriptsize\bfseries]{#1};}}%
\begin{tikzpicture}[font=\footnotesize,>=Stealth,
  base/.style={rounded corners=4pt,align=center,inner sep=5pt,
               drop shadow={shadow xshift=1pt,shadow yshift=-1pt,opacity=0.18}},
  q/.style={base,draw=black!45,fill=black!4,text width=11.4cm},
  f/.style={base,draw=cFind!75,line width=0.7pt,fill=cFind!5,text width=4.3cm,minimum height=1.55cm},
  cc/.style={base,draw=cEval!75,line width=0.9pt,fill=cEval!8,text width=8.2cm},
  probe/.style={rounded corners=3pt,draw=cBase!75,fill=cBase!8,align=center,text width=3.9cm,inner sep=3pt,font=\scriptsize,
                drop shadow={shadow xshift=0.8pt,shadow yshift=-0.8pt,opacity=0.16}},
  ed/.style={-{Stealth[length=5pt,width=5pt]},line width=1.1pt,draw=black!40},
  dd/.style={-{Stealth[length=4pt,width=4pt]},line width=1pt,draw=cBase!70,dashed},
  lbl/.style={font=\scriptsize\itshape,text=black!55,fill=white,inner sep=1pt}]
% column x-centres
\def\cA{-4.85}\def\cB{0}\def\cC{4.85}
% dispute (full-width top)
\node[q] (Q) at (\cB,0) {Two camps: \emph{simple beats FM} (skeptics) vs.\ \emph{FM wins} (boosters) --- leaderboards can't adjudicate};
% dose--response probe headers
\node[probe] (P1) at (\cA,-1.75) {dose--response \#1\\ \textbf{vocabulary}};
\node[probe] (P2) at (\cB,-1.75) {dose--response \#2\\ \textbf{batch-shift}};
\node[probe] (P3) at (\cC,-1.75) {dose--response \#3\\ \textbf{spatial}};
% three findings, side by side
\node[f] (F1) at (\cA,-3.75) {{\fbadge{1}}~\textbf{Parity} --- no advantage, no deficit ($=$ PCA on matched atlases; $+0.0025$, $\pm0.02$)\\[2pt]{\scriptsize\itshape apparent win $=$ label circularity $+$ vocab.\ threshold}};
\node[f] (F2) at (\cB,-3.75) {{\fbadge{2}}~\textbf{General collapse} --- an exchangeability failure, not FM-specific ($\rho{=}0.82$; scATAC flat)};
\node[f] (F3) at (\cC,-3.75) {{\fbadge{3}}~\textbf{Not scale} --- set by task and metric, not model size ($104{\to}316$M: no effect)};
% deliverable (full-width bottom)
\node[cc] (CC) at (\cB,-5.85) {\textbf{Deliverable} --- a reusable, falsifiable evaluation instrument, not a verdict on FMs};
% flows: dispute -> probes (labelled once), probes -> findings, findings -> deliverable
\draw[ed] (Q.south) -- (P2.north) node[lbl,midway]{fair re-check ($k$NN $+$ ref-free $R^2_{\mathrm{expr}}$)};
\draw[ed] (Q.south) to[out=-150,in=90] (P1.north);
\draw[ed] (Q.south) to[out=-30,in=90] (P3.north);
\draw[dd] (P1) -- (F1.north);
\draw[dd] (P2) -- (F2.north);
\draw[dd] (P3) -- (F3.north);
\draw[ed] (F2.south) -- (CC.north);
\draw[ed] (F1.south) to[out=-90,in=150] (CC.north west);
\draw[ed] (F3.south) to[out=-90,in=30] (CC.north east);
% panel label. The roadmap's leftmost content (finding F1 / the converging arrows) sits ~26pt left
% of where panel A's tag lands (A is pinned by the overview's data column), so shift B right by that
% amount to share one vertical A/B column. Both panels are \resizebox'd to 0.92\textwidth in main.tex.
% xshift=0pt (was 26pt in the IEEE figure*): here both panels are \resizebox'd to \linewidth and
% centred, so both bounding-box NW corners share the same x -- match panel A's xshift=0 so the A/B
% tags align vertically. Only the label moves; no other item is repositioned.
\node[anchor=south west,font=\normalsize\bfseries] at ([xshift=0pt,yshift=2pt]current bounding box.north west) {B};
\end{tikzpicture}}
